module.exports = (app) => {
    
    const urls = require('../controllers/products.controller.js');
    
    
    // app.post("/api/urls", urls.createNewUrl);

    // app.get("/api/urls/:id", urls.getRouteById);
    // app.get("/api/urls", urls.getAllUrls);
    
    }